/**
 * @module CoreExports
 * @description APP_NAME için genel örnek export. UI, helper veya başka şeyler olabilir.
 */

// Örnek bir sabit
export const APP_NAME = "APP_NAME";

// Örnek bir fonksiyon
export function greet(userName: string) {
  return `Merhaba, ${userName}!`;
}

// Örnek bir obje
export const CONFIG = {
  version: "1.0.0",
  environment: "development",
};
